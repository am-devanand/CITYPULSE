[build]
  command = "cd frontend && npm ci && npm run build"
  publish = "frontend/dist"

# SPA redirect — serve index.html for all client-side routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
